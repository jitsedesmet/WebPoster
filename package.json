{
  "name": "web-poster",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "build-pdf": "google-chrome --headless --no-sandbox --disable-gpu --disable-dev-shm-usage --no-pdf-header-footer --print-to-pdf=dist/poster.pdf file://$(pwd)/dist/index.html"
  },
  "devDependencies": {
    "sass-embedded": "^1.93.2",
    "vite": "^7.1.7",
    "vite-plugin-singlefile": "^2.3.2"
  },
  "dependencies": {
    "@fortawesome/fontawesome-free": "^7.1.0"
  }
}
